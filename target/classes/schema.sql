CREATE TABLE tenant (
   id int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
   name varchar(64) NOT NULL,
   email varchar(64) NOT NULL,
   phoneNumber varchar(64) NOT NULL,
   eircode varchar(64) NOT NULL
);